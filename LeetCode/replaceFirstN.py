result = text.replace("Python", "JavaScript", 2)
# The count parameter in .replace() follows a strict left-to-right order.
# 1. Scans the string frmo left to right.
# 2. Replaces the first 2 occurrences of "Python" that it finds.
# 3. Leaves any further occurences unchanged. 
